name: Hello Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f3002443@ds.study.iitm.ac.in
        run: echo "Hello, world!"
